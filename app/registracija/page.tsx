import { AuthCard } from '@/components/auth/auth-card'

export const metadata = { title: 'Registracija — balkan.works' }

export default function RegisterPage() {
  return <AuthCard mode="register" />
}
