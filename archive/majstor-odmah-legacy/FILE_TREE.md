# Balkan.works File Tree

The requested tree is present under `balkanworks/`: `apps/mobile`, `apps/web`, `apps/admin`, `services/api`, `packages/ui`, `packages/database`, `packages/config`, `packages/types`, `infrastructure/docker` and root environment/build files. Future services for AI, payments, notifications and analytics are added only in their roadmap phase.
