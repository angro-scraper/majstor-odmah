import { AppShell } from "../../../src/components/app-shell";
import { Button, Input } from "../../../src/components/ui";
export default function BusinessCreationPage() { const steps = ["Naziv", "Kategorija", "Lokacija", "Usluge", "Slike"]; return <AppShell active=""><section className="page-heading"><p>POSLOVNI PROFIL</p><h1>Napravi profil firme</h1><p>Pet jasnih koraka do profila spremnog za verifikaciju.</p></section><section className="creation-layout"><aside>{steps.map((step, index) => <div className={index === 0 ? "step active" : "step"} key={step}><span>{index + 1}</span>{step}</div>)}</aside><form className="creation-form"><p className="label">KORAK 1 OD 5</p><h2>Kako se zove vaša firma?</h2><label>Naziv firme<Input placeholder="npr. Auto Servis Marković" /></label><label>Kratak opis<Input placeholder="Čime se bavite?" /></label><div className="form-actions"><a href="/dashboard"><Button className="secondary" type="button">Sačuvaj kasnije</Button></a><Button type="button">Nastavi →</Button></div></form></section></AppShell>; }
