FROM node:22-alpine

WORKDIR /workspace

RUN corepack enable

COPY . .

RUN pnpm install --no-frozen-lockfile

EXPOSE 3000

CMD ["pnpm", "--filter", "@balkanworks/web", "exec", "next", "dev", "--hostname", "0.0.0.0"]
